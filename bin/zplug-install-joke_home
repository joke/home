#!/bin/zsh
cd ~/
if [[ ! -e ~/.git ]] {
	git init
	git remote add origin git@github.com:joke/home.git
	git remote add zplug ${${:-${ZPLUG_REPOS}/joke/home}#$PWD/}
}
git fetch zplug
git reset --keep zplug/master
